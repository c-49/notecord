VITE_DIRECTUS_URL=http://localhost:8055
# Must match ADMIN_TOKEN in backend/.env
VITE_DIRECTUS_TOKEN=replace_with_long_random_token
# Must match ASSET_TOKEN in backend/.env — used only for file/image URLs
VITE_DIRECTUS_ASSET_TOKEN=replace_with_long_random_token  
